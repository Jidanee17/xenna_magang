<?xml version="1.0" encoding="utf-8"?>
<odoo>


   <record id="view_equipment_item_list" model="ir.ui.view">
        <field name="name">equipment.item.list</field>
        <field name="model">equipment.item</field>
        <field name="arch" type="xml">
            <list string="Equipment Items">
                <field name="code"/>
                <field name="name"/>
                <field name="category"/>
                <field name="state" decoration-success="state == 'available'"
                       decoration-warning="state == 'on_loan'"
                       decoration-danger="state == 'damaged'"/>
            </list>
        </field>
    </record>

    <record id="view_equipment_item_form" model="ir.ui.view">
        <field name="name">equipment.item.form</field>
        <field name="model">equipment.item</field>
        <field name="arch" type="xml">
            <form string="Equipment Item">
                <sheet>
                    <group>
                        <group>
                            <field name="code" readonly="1"/>
                            <field name="name"/>
                            <field name="category"/>
                        </group>
                        <group>
                            <field name="state"/>
                        </group>
                    </group>
                    <group>
                        <field name="notes" placeholder="Catatan tambahan mengenai alat..."/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_equipment_item_search" model="ir.ui.view">
        <field name="name">equipment.item.search</field>
        <field name="model">equipment.item</field>
        <field name="arch" type="xml">
            <search string="Search Equipment Items">
                <field name="name"/>
                <field name="code"/>
                <field name="category"/>

                <filter
                    string="Available"
                    name="filter_available"
                    domain="[('state', '=', 'available')]"
                />
                <filter
                    string="On Loan"
                    name="filter_on_loan"
                    domain="[('state', '=', 'on_loan')]"
                />
                <filter
                    string="Damaged"
                    name="filter_damaged"
                    domain="[('state', '=', 'damaged')]"
                />

                <separator/>

                <filter
                    string="Perangkat Elektronik"
                    name="filter_elektronik"
                    domain="[('category', '=', 'elektronik')]"
                />
                <filter
                    string="Peralatan Kantor"
                    name="filter_kantor"
                    domain="[('category', '=', 'kantor')]"
                />
                <filter
                    string="Lainnya"
                    name="filter_lainnya"
                    domain="[('category', '=', 'lainnya')]"
                />

                <group expand="0" string="Group By">
                    <filter
                        string="Status"
                        name="groupby_state"
                        context="{'group_by': 'state'}"
                    />
                    <filter
                        string="Kategori"
                        name="groupby_category"
                        context="{'group_by': 'category'}"
                    />
                </group>
            </search>
        </field>
    </record>

    <record id="action_equipment_item" model="ir.actions.act_window">
        <field name="name">Equipment Items</field>
        <field name="res_model">equipment.item</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="view_equipment_item_search"/>
        <field name="context">{'search_default_filter_available': 1}</field>
    </record>
</odoo>